# 黑白模式 + 多语言（i18n）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 GoGBA 纯静态文档站增加深/浅双主题（accent 沿用图标紫）和英/简/繁三语，自动检测系统偏好并支持手动切换，首屏无闪烁。

**Architecture:** 纯客户端实现。`<head>` 内联同步脚本在 CSS/内容渲染前确定 `data-theme` 与 `lang`（防 FOUC）；CSS 变量分浅色 `:root` 与 `[data-theme="dark"]` 两组；`js/theme.js` 与 `js/i18n.js` 负责切换与渲染；HTML 文本以 `data-i18n` 标注、保留英文兜底。6 个页面共用一套逻辑。

**Tech Stack:** 原生 HTML / CSS（CSS 变量）/ 原生 JS（ES2015，无构建、无依赖），GitHub Pages 纯静态托管（`.nojekyll`）。

**验证说明:** 本仓库无自动化测试框架。每个任务的"验证"为浏览器 + DevTools 的手动观察步骤，描述精确的操作与预期结果。每个任务结束都提交。

---

## 文件结构

| 文件 | 责任 |
|------|------|
| `styles.css` | 颜色变量重构为双主题；新增主题切换按钮、语言下拉的样式 |
| `js/theme.js`（新建） | 主题判定/切换/系统跟随 |
| `js/i18n.js`（新建） | 语言字典 + 检测/渲染/切换 |
| `js/common.js` | 在 `init()` 接入 theme + i18n 初始化 |
| `index.html` 等 6 个页面 | `<head>` 内联防闪烁脚本；导航栏控件；文本标注 `data-i18n` |
| `_config.yml` | 删除（清理与 `.nojekyll` 的矛盾） |

页面清单：`index.html`、`about.html`、`support.html`、`privacy-policy.html`、`terms-of-service.html`、`license.html`。

---

## Task 1: 清理部署矛盾（删除 _config.yml）

**Files:**
- Delete: `_config.yml`

- [ ] **Step 1: 确认 `.nojekyll` 存在**

Run: `ls -la .nojekyll`
Expected: 文件存在（站点实际走纯静态，Jekyll 配置无效）。

- [ ] **Step 2: 删除 `_config.yml`**

Run: `git rm _config.yml`
Expected: 删除成功。

- [ ] **Step 3: 提交**

```bash
git commit -m "chore: remove unused Jekyll config (site is pure static via .nojekyll)"
```

---

## Task 2: CSS 双主题变量重构（浅色默认 + 深色）

将现有单一深色变量重构为「浅色默认 `:root` + `[data-theme="dark"]`」两组，统一 accent 命名，accent 双模式共用图标紫派生值。

**Files:**
- Modify: `styles.css:42-51`（`:root` 变量块）

- [ ] **Step 1: 替换 `:root` 变量块为双主题定义**

将 `styles.css` 第 42-51 行的整个 `:root { ... }` 块替换为：

```css
:root {
    /* Accent — 应用图标紫，浅色模式用稍深派生值保证对比度 */
    --accent: #7c5fd6;
    --accent-hover: #6a4fc0;
    --accent-color: var(--accent); /* 兼容历史行内样式引用 */

    /* Light theme (default) */
    --bg-color: #ffffff;
    --bg-secondary: #f5f5f7;
    --text-primary: #1a1a1a;
    --text-secondary: #555555;
    --border-color: #e2e2e8;
    --card-bg: #f5f5f7;
    --hover-bg: #ececf2;

    /* 历史命名兼容 */
    --primary-color: var(--accent);
    --primary-dark: var(--accent-hover);
}

[data-theme="dark"] {
    /* 深色模式 accent 用稍亮派生值 */
    --accent: #8B6FE0;
    --accent-hover: #9d85e8;
    --accent-color: var(--accent);

    --bg-color: #0f172a;
    --bg-secondary: #1e293b;
    --text-primary: #f1f5f9;
    --text-secondary: #cbd5e1;
    --border-color: #334155;
    --card-bg: #1e293b;
    --hover-bg: #334155;

    --primary-color: var(--accent);
    --primary-dark: var(--accent-hover);
}
```

- [ ] **Step 2: 更新背景光晕使用 accent，且仅深色显示**

定位 `styles.css` 中 `body::before` 的 `background:` 渐变（约第 78-83 行，含 `rgba(99, 102, 241, ...)` 等硬编码紫）。将其替换为基于 accent、且在浅色下更克制的写法：

```css
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background:
        radial-gradient(circle at 20% 50%, rgba(124, 95, 214, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(124, 95, 214, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 40% 20%, rgba(124, 95, 214, 0.03) 0%, transparent 50%);
    animation: gradientShift 20s ease infinite;
    pointer-events: none;
    z-index: -1;
}

[data-theme="dark"] body::before {
    background:
        radial-gradient(circle at 20% 50%, rgba(139, 111, 224, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(139, 111, 224, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 40% 20%, rgba(139, 111, 224, 0.04) 0%, transparent 50%);
}
```

注意：保留原有 `body::before` 的非 `background` 属性，只调整 `background` 并新增深色覆盖块。若原块包含其它属性以现有为准合并。

- [ ] **Step 3: 平滑过渡（避免切换时生硬跳变）**

在 `styles.css` 的 `body { ... }` 规则中追加：

```css
    transition: background-color 0.3s ease, color 0.3s ease;
```

- [ ] **Step 4: 浏览器验证两种主题渲染**

操作：浏览器打开 `index.html`，DevTools Elements 选中 `<html>`。
- 默认（无 data-theme）应为浅色：白底深字。
- 在 `<html>` 上手动加 `data-theme="dark"`，应立即变深色蓝底浅字，accent 按钮呈亮紫。
Expected: 两种状态文字可读、按钮/链接为紫色、无破版。

- [ ] **Step 5: 提交**

```bash
git add styles.css
git commit -m "feat: refactor CSS to light/dark dual theme with icon-purple accent"
```

---

## Task 3: 主题逻辑（js/theme.js）

实现主题判定（localStorage > 系统 > 浅色）、手动切换、系统跟随。此任务先不接 UI 按钮（Task 4 加按钮），但暴露 `window.toggleTheme` 供按钮调用。

**Files:**
- Create: `js/theme.js`

- [ ] **Step 1: 创建 `js/theme.js`**

```js
// Theme handling: localStorage > system preference > light default
(function () {
    'use strict';

    var STORAGE_KEY = 'theme';

    function getStoredTheme() {
        try { return localStorage.getItem(STORAGE_KEY); } catch (e) { return null; }
    }

    function systemPrefersDark() {
        return window.matchMedia &&
            window.matchMedia('(prefers-color-scheme: dark)').matches;
    }

    function resolveTheme() {
        var stored = getStoredTheme();
        if (stored === 'light' || stored === 'dark') return stored;
        return systemPrefersDark() ? 'dark' : 'light';
    }

    function applyTheme(theme) {
        document.documentElement.setAttribute('data-theme', theme);
        updateToggleButton(theme);
    }

    function updateToggleButton(theme) {
        var btn = document.querySelector('.theme-toggle');
        if (!btn) return;
        var isDark = theme === 'dark';
        // 显示"将切换到的目标"图标
        btn.textContent = isDark ? '☀️' : '🌙'; // ☀️ / 🌙
        btn.setAttribute('aria-label', isDark ? 'Switch to light mode' : 'Switch to dark mode');
    }

    function toggleTheme() {
        var current = document.documentElement.getAttribute('data-theme') || resolveTheme();
        var next = current === 'dark' ? 'light' : 'dark';
        try { localStorage.setItem(STORAGE_KEY, next); } catch (e) {}
        applyTheme(next);
    }

    function initTheme() {
        applyTheme(resolveTheme());
        // 用户未手动选择时，实时跟随系统变化
        if (window.matchMedia) {
            var mq = window.matchMedia('(prefers-color-scheme: dark)');
            var onChange = function () {
                if (!getStoredTheme()) applyTheme(systemPrefersDark() ? 'dark' : 'light');
            };
            if (mq.addEventListener) mq.addEventListener('change', onChange);
            else if (mq.addListener) mq.addListener(onChange);
        }
    }

    window.GoGBATheme = { init: initTheme, toggle: toggleTheme };
})();
```

- [ ] **Step 2: 语法自检**

Run: `node --check js/theme.js`
Expected: 无输出（语法正确）。

- [ ] **Step 3: 提交**

```bash
git add js/theme.js
git commit -m "feat: add theme.js (resolve/apply/toggle/system-follow logic)"
```

---

## Task 4: head 内联防闪烁脚本 + 导航栏主题按钮（index.html 先行）

先在 `index.html` 跑通：内联脚本提前设 `data-theme`，导航加按钮，引入 `theme.js`。

**Files:**
- Modify: `index.html`（`<head>` 内、`.nav-menu`、底部脚本区）

- [ ] **Step 1: 在 `<head>` 的 `<meta charset>` 之后插入内联防闪烁脚本**

定位 `index.html:4`（`<meta charset="UTF-8">`），在其下一行插入：

```html
    <script>
      (function () {
        try {
          var t = localStorage.getItem('theme');
          if (t !== 'light' && t !== 'dark') {
            t = (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) ? 'dark' : 'light';
          }
          document.documentElement.setAttribute('data-theme', t);
        } catch (e) {}
      })();
    </script>
```

- [ ] **Step 2: 在 `.nav-menu` 列表末尾插入主题切换按钮**

定位 `index.html` 中 `</ul>`（`.nav-menu` 结束，约第 133 行），在该 `</ul>` 之前插入：

```html
                    <li class="nav-controls">
                        <button class="theme-toggle" type="button" aria-label="Toggle theme" onclick="GoGBATheme.toggle()"></button>
                    </li>
```

- [ ] **Step 3: 在底部 `common.js` 之前引入 `theme.js`**

定位 `index.html:371`（`<script src="js/common.js"></script>`），在其上方插入：

```html
    <script src="js/theme.js"></script>
```

- [ ] **Step 4: 在 common.js init 接入主题初始化（一次性，全站受益）**

修改 `js/common.js` 的 `init()` 函数（约第 187-194 行），在函数体开头加入：

```js
    if (window.GoGBATheme) window.GoGBATheme.init();
```

放在 `initMobileMenu();` 之前。

- [ ] **Step 5: 主题按钮样式**

在 `styles.css` 末尾追加：

```css
/* Nav controls: theme toggle + language switch */
.nav-controls {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.theme-toggle {
    background: transparent;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    color: var(--text-primary);
    cursor: pointer;
    font-size: 1rem;
    line-height: 1;
    padding: 0.35rem 0.5rem;
    transition: background-color 0.2s ease, border-color 0.2s ease;
}

.theme-toggle:hover {
    background-color: var(--hover-bg);
    border-color: var(--accent);
}
```

- [ ] **Step 6: 浏览器验证防闪烁与切换**

操作：
1. DevTools > Rendering 或系统设置切到深色，打开 `index.html` → 首屏应直接深色，无白色闪一下。
2. 点击导航按钮 → 切浅色，刷新仍为浅色（localStorage 生效）。
3. DevTools Application > Local Storage 应有 `theme`。
4. 删除该 localStorage 项并刷新 → 回到跟随系统。
Expected: 全部符合；切换时按钮图标在 ☀️/🌙 间变化。

- [ ] **Step 7: 提交**

```bash
git add index.html styles.css js/common.js
git commit -m "feat: wire theme toggle + anti-FOUC inline script on index"
```

---

## Task 5: 主题接入其余 5 个页面

对 `about.html`、`support.html`、`privacy-policy.html`、`terms-of-service.html`、`license.html` 各自做 Task 4 的 Step 1 / Step 2 / Step 3（内联脚本、导航按钮、引入 theme.js）。common.js 已全局接入，无需再改。

**Files:**
- Modify: `about.html`、`support.html`、`privacy-policy.html`、`terms-of-service.html`、`license.html`

- [ ] **Step 1: 每个页面在 `<meta charset="UTF-8">` 下方插入内联防闪烁脚本**

对上述 5 个文件，逐个在 charset meta 之后插入与 Task 4 Step 1 完全相同的 `<script>` 块：

```html
    <script>
      (function () {
        try {
          var t = localStorage.getItem('theme');
          if (t !== 'light' && t !== 'dark') {
            t = (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) ? 'dark' : 'light';
          }
          document.documentElement.setAttribute('data-theme', t);
        } catch (e) {}
      })();
    </script>
```

- [ ] **Step 2: 每个页面在 `.nav-menu` 的 `</ul>` 前插入主题按钮**

对每个文件，在 `.nav-menu` 结束的 `</ul>` 之前插入：

```html
                    <li class="nav-controls">
                        <button class="theme-toggle" type="button" aria-label="Toggle theme" onclick="GoGBATheme.toggle()"></button>
                    </li>
```

- [ ] **Step 3: 每个页面在 `js/common.js` 引入行上方插入 theme.js**

对每个文件，在 `<script src="js/common.js"></script>` 上方插入：

```html
    <script src="js/theme.js"></script>
```

- [ ] **Step 4: 验证 5 个页面主题一致**

操作：依次打开 5 个页面，点击主题按钮，确认主题切换、刷新保持、按钮图标正确，且与 index 行为一致。
Expected: 5 页全部正常，无破版。

- [ ] **Step 5: 提交**

```bash
git add about.html support.html privacy-policy.html terms-of-service.html license.html
git commit -m "feat: wire theme toggle on about/support/legal pages"
```

---

## Task 6: i18n 核心（js/i18n.js）+ 字典骨架（仅 nav/footer/home 共用键）

先建立 i18n 引擎和检测/渲染/切换逻辑，字典先填全站共用的 `nav.*`、`footer.*` 和首页 `home.*` 键，跑通机制。其余页面键在 Task 8 补充。

**Files:**
- Create: `js/i18n.js`

- [ ] **Step 1: 创建 `js/i18n.js`（引擎 + en/zh-Hans/zh-Hant 的 nav/footer/home 键）**

```js
// i18n: dictionary + detect/render/switch. localStorage > navigator.language > en
(function () {
    'use strict';

    var STORAGE_KEY = 'lang';
    var SUPPORTED = ['en', 'zh-Hans', 'zh-Hant'];

    var I18N = {
        'en': {
            'nav.home': 'Home',
            'nav.about': 'About',
            'nav.support': 'Support',
            'nav.privacy': 'Privacy Policy',
            'nav.terms': 'Terms of Service',
            'nav.license': 'License',
            'footer.rights': '© 2026 GoGBA. All rights reserved.',
            'home.hero.subtitle': 'A quiet, reliable, and immersive handheld emulator',
            'home.hero.description': 'Play Game Boy Advance, Game Boy Color, and Game Boy classics with optimized touch controls, seamless saves, RetroAchievements integration, and full gamepad support.',
            'home.features.title': 'Key Features',
            'home.feature.seamless.title': 'Seamless Experience',
            'home.feature.seamless.desc': 'Open and continue playing instantly. Your saves are always safe.',
            'home.feature.save.title': 'Reliable Save System',
            'home.feature.save.desc': 'Automatic battery-save writes every 30 seconds while you play, plus saves on exit and when backgrounded.',
            'home.feature.touch.title': 'Optimized Touch Controls',
            'home.feature.touch.desc': 'Normal and Arcade control modes with haptic feedback.',
            'home.feature.gamepad.title': 'Gamepad Support',
            'home.feature.gamepad.desc': 'Full controller support with customizable button mapping.',
            'home.feature.ra.title': 'RetroAchievements',
            'home.feature.ra.desc': 'Unlock achievements and compete on leaderboards.',
            'home.feature.themes.title': 'Dark Themes',
            'home.feature.themes.desc': '10 carefully designed dark themes for eye comfort.',
            'home.demo.title': 'Demo Games',
            'home.demo.name': 'A Rushed Hack Job',
            'home.demo.desc': 'A GBA-format homebrew demo ROM to try GoGBA (also supports GBC and GB titles).',
            'home.demo.download': 'Download Demo Game',
            'home.contact.title': 'Contact & Support',
            'home.contact.discord': 'Join Discord'
        },
        'zh-Hans': {
            'nav.home': '首页',
            'nav.about': '关于',
            'nav.support': '支持',
            'nav.privacy': '隐私政策',
            'nav.terms': '服务条款',
            'nav.license': '许可证',
            'footer.rights': '© 2026 GoGBA。保留所有权利。',
            'home.hero.subtitle': '安静、可靠、沉浸的掌机模拟器',
            'home.hero.description': '流畅畅玩 Game Boy Advance、Game Boy Color 与 Game Boy 经典游戏，支持优化的触控操作、无缝存档、RetroAchievements 成就集成与完整手柄支持。',
            'home.features.title': '核心功能',
            'home.feature.seamless.title': '无缝体验',
            'home.feature.seamless.desc': '随开随玩，存档始终安全。',
            'home.feature.save.title': '可靠存档系统',
            'home.feature.save.desc': '游戏中每 30 秒自动写入电池存档，退出和切到后台时也会保存。',
            'home.feature.touch.title': '优化触控操作',
            'home.feature.touch.desc': '提供普通与街机两种操作模式，带触觉反馈。',
            'home.feature.gamepad.title': '手柄支持',
            'home.feature.gamepad.desc': '完整手柄支持，按键可自定义映射。',
            'home.feature.ra.title': 'RetroAchievements',
            'home.feature.ra.desc': '解锁成就，同台竞技排行榜。',
            'home.feature.themes.title': '深色主题',
            'home.feature.themes.desc': '10 套精心设计的深色主题，护眼舒适。',
            'home.demo.title': '演示游戏',
            'home.demo.name': 'A Rushed Hack Job',
            'home.demo.desc': '一个 GBA 格式的自制演示 ROM，用于试玩 GoGBA（同时支持 GBC 与 GB 游戏）。',
            'home.demo.download': '下载演示游戏',
            'home.contact.title': '联系与支持',
            'home.contact.discord': '加入 Discord'
        },
        'zh-Hant': {
            'nav.home': '首頁',
            'nav.about': '關於',
            'nav.support': '支援',
            'nav.privacy': '隱私政策',
            'nav.terms': '服務條款',
            'nav.license': '授權條款',
            'footer.rights': '© 2026 GoGBA。保留所有權利。',
            'home.hero.subtitle': '安靜、可靠、沉浸的掌機模擬器',
            'home.hero.description': '流暢暢玩 Game Boy Advance、Game Boy Color 與 Game Boy 經典遊戲，支援優化的觸控操作、無縫存檔、RetroAchievements 成就整合與完整手柄支援。',
            'home.features.title': '核心功能',
            'home.feature.seamless.title': '無縫體驗',
            'home.feature.seamless.desc': '隨開隨玩，存檔始終安全。',
            'home.feature.save.title': '可靠存檔系統',
            'home.feature.save.desc': '遊戲中每 30 秒自動寫入電池存檔，退出和切到背景時也會保存。',
            'home.feature.touch.title': '優化觸控操作',
            'home.feature.touch.desc': '提供普通與街機兩種操作模式，帶觸覺回饋。',
            'home.feature.gamepad.title': '手柄支援',
            'home.feature.gamepad.desc': '完整手柄支援，按鍵可自訂義映射。',
            'home.feature.ra.title': 'RetroAchievements',
            'home.feature.ra.desc': '解鎖成就，同台競技排行榜。',
            'home.feature.themes.title': '深色主題',
            'home.feature.themes.desc': '10 套精心設計的深色主題，護眼舒適。',
            'home.demo.title': '演示遊戲',
            'home.demo.name': 'A Rushed Hack Job',
            'home.demo.desc': '一個 GBA 格式的自製演示 ROM，用於試玩 GoGBA（同時支援 GBC 與 GB 遊戲）。',
            'home.demo.download': '下載演示遊戲',
            'home.contact.title': '聯繫與支援',
            'home.contact.discord': '加入 Discord'
        }
    };

    function getStoredLang() {
        try { return localStorage.getItem(STORAGE_KEY); } catch (e) { return null; }
    }

    function mapNavigatorLang() {
        var l = (navigator.language || navigator.userLanguage || 'en').toLowerCase();
        if (l.indexOf('zh') === 0) {
            if (l.indexOf('tw') > -1 || l.indexOf('hk') > -1 || l.indexOf('mo') > -1 || l.indexOf('hant') > -1) {
                return 'zh-Hant';
            }
            return 'zh-Hans'; // zh-cn, zh-sg, zh-hans, bare zh
        }
        return 'en';
    }

    function resolveLang() {
        var stored = getStoredLang();
        if (SUPPORTED.indexOf(stored) > -1) return stored;
        return mapNavigatorLang();
    }

    function t(lang, key) {
        var dict = I18N[lang] || I18N.en;
        return (key in dict) ? dict[key] : (I18N.en[key] !== undefined ? I18N.en[key] : null);
    }

    function render(lang) {
        // 文本节点
        var nodes = document.querySelectorAll('[data-i18n]');
        for (var i = 0; i < nodes.length; i++) {
            var key = nodes[i].getAttribute('data-i18n');
            var val = t(lang, key);
            if (val !== null) nodes[i].textContent = val;
        }
        // 富文本（站内静态白名单，无用户输入）
        var htmlNodes = document.querySelectorAll('[data-i18n-html]');
        for (var j = 0; j < htmlNodes.length; j++) {
            var hkey = htmlNodes[j].getAttribute('data-i18n-html');
            var hval = t(lang, hkey);
            if (hval !== null) htmlNodes[j].innerHTML = hval;
        }
        // 属性
        var attrNodes = document.querySelectorAll('[data-i18n-attr]');
        for (var k = 0; k < attrNodes.length; k++) {
            var spec = attrNodes[k].getAttribute('data-i18n-attr');
            var pairs = spec.split(';');
            for (var p = 0; p < pairs.length; p++) {
                var pair = pairs[p].split(':');
                if (pair.length === 2) {
                    var av = t(lang, pair[1]);
                    if (av !== null) attrNodes[k].setAttribute(pair[0], av);
                }
            }
        }
        document.documentElement.setAttribute('lang', lang === 'en' ? 'en' : lang);
        updateSwitcher(lang);
    }

    function updateSwitcher(lang) {
        var sel = document.querySelector('.lang-switch');
        if (sel && sel.value !== lang) sel.value = lang;
    }

    function setLang(lang) {
        if (SUPPORTED.indexOf(lang) === -1) return;
        try { localStorage.setItem(STORAGE_KEY, lang); } catch (e) {}
        render(lang);
    }

    function initI18n() {
        render(resolveLang());
        var sel = document.querySelector('.lang-switch');
        if (sel) {
            sel.addEventListener('change', function () { setLang(sel.value); });
        }
    }

    window.GoGBAI18n = { init: initI18n, setLang: setLang, resolve: resolveLang, SUPPORTED: SUPPORTED };
})();
```

- [ ] **Step 2: 语法自检**

Run: `node --check js/i18n.js`
Expected: 无输出。

- [ ] **Step 3: 提交**

```bash
git add js/i18n.js
git commit -m "feat: add i18n.js engine + en/zh-Hans/zh-Hant dictionary (nav/footer/home)"
```

---

## Task 7: 首页 i18n 接入（语言下拉 + data-i18n 标注 + head 内联 lang）

**Files:**
- Modify: `index.html`、`js/common.js`、`styles.css`

- [ ] **Step 1: head 内联脚本补充提前设 lang**

在 `index.html` 已插入的主题内联脚本（Task 4 Step 1）的 `<script>` 块内、`document.documentElement.setAttribute('data-theme', t);` 之后追加 lang 提前设置：

```js
          var L = localStorage.getItem('lang');
          if (L !== 'en' && L !== 'zh-Hans' && L !== 'zh-Hant') {
            var nl = (navigator.language || 'en').toLowerCase();
            if (nl.indexOf('zh') === 0) {
              L = (nl.indexOf('tw') > -1 || nl.indexOf('hk') > -1 || nl.indexOf('mo') > -1 || nl.indexOf('hant') > -1) ? 'zh-Hant' : 'zh-Hans';
            } else { L = 'en'; }
          }
          document.documentElement.setAttribute('lang', L);
```

- [ ] **Step 2: 导航加语言下拉**

在 `index.html` 的 `.nav-controls` `<li>` 内（Task 4 Step 2 插入的），主题按钮之后追加：

```html
                        <select class="lang-switch" aria-label="Language">
                            <option value="en">EN</option>
                            <option value="zh-Hans">简体中文</option>
                            <option value="zh-Hant">繁體中文</option>
                        </select>
```

注意：`option` 文本里的转义为占位说明，实际写入文件应为可读中文「简体中文」「繁體中文」。

- [ ] **Step 3: 给导航菜单项加 data-i18n**

将 `index.html` 的 `.nav-menu` 各链接（约 127-132 行）改为带 `data-i18n`：

```html
                    <li><a href="index.html" class="active" data-i18n="nav.home">Home</a></li>
                    <li><a href="about.html" data-i18n="nav.about">About</a></li>
                    <li><a href="support.html" data-i18n="nav.support">Support</a></li>
                    <li><a href="privacy-policy.html" data-i18n="nav.privacy">Privacy Policy</a></li>
                    <li><a href="terms-of-service.html" data-i18n="nav.terms">Terms of Service</a></li>
                    <li><a href="license.html" data-i18n="nav.license">License</a></li>
```

- [ ] **Step 4: 给 hero / features / demo / contact / footer 文本加 data-i18n**

按下列映射，给 `index.html` 对应元素加属性（保留原英文为兜底）：

- `<p class="hero-subtitle">` → `data-i18n="home.hero.subtitle"`
- `<p class="hero-description">` → `data-i18n="home.hero.description"`
- features 区 `<h2>Key Features</h2>` → `data-i18n="home.features.title"`
- 6 个 `.feature-card` 的 `<h3>`/`<p>` 依次：
  - Seamless：`home.feature.seamless.title` / `home.feature.seamless.desc`
  - Reliable Save：`home.feature.save.title` / `home.feature.save.desc`
  - Touch：`home.feature.touch.title` / `home.feature.touch.desc`
  - Gamepad：`home.feature.gamepad.title` / `home.feature.gamepad.desc`
  - RetroAchievements：`home.feature.ra.title` / `home.feature.ra.desc`
  - Dark Themes：`home.feature.themes.title` / `home.feature.themes.desc`
- demo 区 `<h2>Demo Games</h2>` → `home.demo.title`；`<h3>A Rushed Hack Job</h3>` → `home.demo.name`；其下 `<p>` → `home.demo.desc`；下载按钮文本 `Download Demo Game` → `home.demo.download`
- contact 区 `<h2>Contact & Support</h2>` → `home.contact.title`；Discord 按钮内 `<span>Join Discord</span>` → `home.contact.discord`
- footer `<p>&copy; 2026 GoGBA. All rights reserved.</p>` → `data-i18n="footer.rights"`；footer 链接同 nav 用 `nav.privacy`/`nav.terms`/`nav.license`/`nav.support`

示例（hero）：

```html
                        <p class="hero-subtitle" data-i18n="home.hero.subtitle">A quiet, reliable, and immersive handheld emulator</p>
                        <p class="hero-description" data-i18n="home.hero.description">Play Game Boy Advance, Game Boy Color, and Game Boy classics with optimized touch controls, seamless saves, RetroAchievements integration, and full gamepad support.</p>
```

注意 Demo 下载按钮当前是 `<a ...>Download Demo Game</a>`，直接在该 `<a>` 上加 `data-i18n="home.demo.download"`。

- [ ] **Step 5: 引入 i18n.js 并在 common.js init 接入**

在 `index.html` 的 `<script src="js/theme.js"></script>` 下方加：

```html
    <script src="js/i18n.js"></script>
```

在 `js/common.js` 的 `init()` 内，`window.GoGBATheme.init();` 之后加：

```js
    if (window.GoGBAI18n) window.GoGBAI18n.init();
```

- [ ] **Step 6: 语言下拉样式**

在 `styles.css` 末尾（Task 4 Step 5 之后）追加：

```css
.lang-switch {
    background: transparent;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    color: var(--text-primary);
    cursor: pointer;
    font-size: 0.85rem;
    padding: 0.35rem 0.5rem;
    transition: border-color 0.2s ease;
}

.lang-switch:hover { border-color: var(--accent); }
.lang-switch option { color: #1a1a1a; }
```

- [ ] **Step 7: 浏览器验证首页三语切换**

操作：
1. 打开 `index.html`，下拉选「简体中文」→ hero/features/footer 等全部变简体，无残留英文。
2. 选「繁體中文」→ 变繁体。
3. 刷新 → 保持上次语言（localStorage `lang`）。
4. DevTools Console 改 `navigator.language` 不便，改为：清除 `lang` localStorage，浏览器/系统语言设为中文区域，刷新 → 自动中文。
5. 检查 `<html lang>` 随语言更新。
Expected: 全部符合；切换无逐字闪动（一次性渲染）。

- [ ] **Step 8: 提交**

```bash
git add index.html js/common.js styles.css
git commit -m "feat: wire i18n on home page (lang switch + data-i18n + anti-FOUC lang)"
```

---

## Task 8: 扩展字典 + 接入其余 5 个页面

为 `about`/`support`/`privacy`/`terms`/`license` 补字典键并标注。每个页面文本量较大，按页逐个处理，保持精简翻译调性，法律页照实翻译、不加"以英文为准"声明。

> 实现者注意：先用 `grep -n` 读取每个页面的可见文本，按其结构补充 `I18N` 三语键（命名空间 `about.*` / `support.*` / `privacy.*` / `terms.*` / `license.*`），再回到 HTML 加 `data-i18n`。共用的导航/footer 已有键，直接复用 `nav.*` / `footer.*`。

**Files:**
- Modify: `js/i18n.js`（追加各页键）
- Modify: `about.html`、`support.html`、`privacy-policy.html`、`terms-of-service.html`、`license.html`

### 8a — about.html

- [ ] **Step 1: 提取文本**

Run: `grep -nE "<(h1|h2|h3|p|li|a|span)" about.html`
Expected: 列出可见文本节点，据此规划 `about.*` 键。

- [ ] **Step 2: 在 `js/i18n.js` 的三个语言对象中各追加 `about.*` 键**

为 about 页每个可见文本块定义 `about.<section>.<field>` 键，三语各一份，值为对应翻译（英文取页面原文，简/繁照实精简翻译）。保持三语 key 集合一致。

- [ ] **Step 3: 给 about.html 文本元素加 `data-i18n`**

对每个文本节点加 `data-i18n="about.xxx"`，保留原英文兜底。导航/footer 复用 `nav.*`/`footer.*`。

- [ ] **Step 4: head 内联 lang + 导航语言下拉**

确认 about.html 已在 Task 5 加了主题内联脚本与按钮；此处对该内联脚本追加 Task 7 Step 1 的 lang 片段，并在 `.nav-controls` 内主题按钮后加 Task 7 Step 2 的 `<select class="lang-switch">` 块，并在 theme.js 下方加 `<script src="js/i18n.js"></script>`。

- [ ] **Step 5: 语法自检 + 浏览器验证**

Run: `node --check js/i18n.js`
操作：打开 about.html，三语切换检查无残留英文、刷新保持。
Expected: 通过。

- [ ] **Step 6: 提交**

```bash
git add js/i18n.js about.html
git commit -m "feat: i18n for about page"
```

### 8b — support.html

- [ ] **Step 1-6: 同 8a 流程**，命名空间用 `support.*`，文件为 `support.html`。提交信息：`feat: i18n for support page`。

### 8c — privacy-policy.html

- [ ] **Step 1-6: 同 8a 流程**，命名空间用 `privacy.*`，法律全文照实翻译、不加"以英文为准"声明。提交信息：`feat: i18n for privacy policy page`。

### 8d — terms-of-service.html

- [ ] **Step 1-6: 同 8a 流程**，命名空间用 `terms.*`。提交信息：`feat: i18n for terms of service page`。

### 8e — license.html

- [ ] **Step 1-6: 同 8a 流程**，命名空间用 `license.*`。提交信息：`feat: i18n for license page`。

---

## Task 9: 全站联合验证

**Files:** 无（仅验证）

- [ ] **Step 1: 三语 × 双主题矩阵验证**

操作：6 个页面 × {浅/深} × {EN/简/繁} 抽查。重点：
- 切语言后导航/footer/正文无残留英文（除品牌名 GoGBA、专有名词如 RetroAchievements/Game Boy）。
- 切主题后文字对比度足够、accent 为紫色。
- 刷新后主题与语言均保持。
- 移动端折叠菜单内主题按钮与语言下拉可用（缩窄窗口验证）。
Expected: 全部正常。

- [ ] **Step 2: 防闪烁验证**

操作：系统深色 + localStorage 设 light，硬刷新各页 → 首屏不应闪深色。语言同理（设 zh-Hans 后刷新不应先英后中跳变）。
Expected: 无明显闪烁。

- [ ] **Step 3: 控制台无报错**

操作：各页 DevTools Console 检查。
Expected: 无 error。

- [ ] **Step 4: 最终提交（若验证中有微调）**

```bash
git add -A
git commit -m "chore: cross-page theme + i18n verification fixes"
```

---

## 完成标准

- 6 页均支持深/浅主题切换，accent 为图标紫，localStorage 记忆，跟随系统，首屏无闪烁。
- 6 页均支持 EN/简/繁，navigator.language 自动检测，下拉手动切换，localStorage 记忆，`<html lang>` 同步。
- `_config.yml` 已删除，纯静态托管不受影响。
- 无构建工具、无第三方依赖引入。
